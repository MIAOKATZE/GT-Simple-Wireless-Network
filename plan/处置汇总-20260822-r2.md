# 处置汇总 · 20260822 第二轮（r2）BUG 批次处置记录

- **生成日期**：2026-08-22
- **分支基线**：`fix/20260822-r2`，基线 HEAD `7fda090`（本文件由批次①②波次 swn-editor-1 创建；批次③④⑤由另一波次并行处置，本文件仅登记本波条目，另一波次可追加其条目段落）
- **本波范围**：批次①（网络包校验 B2-03/12）+ 批次②（信息屏 TE B2-02/09/11）+ 登记项（B2-01 转并入 O2-B07、B2-10 降级不实施）
- **输入依据**：`runs/r2-20260822/results/swn-survey-a` 深查报告（7 条目，基线 7fda090 实测锚点零漂移）
- **硬约束合规**（plan 3.4-h）：NBT/S35 键逐字不动——writeSyncData/readSyncData 本体、getDescriptionPacket/onDataPacket、物品 NBT 键常量均未触碰；updateEntity 五段顺序未动（B2-02 仅改第 5 段被调方法 `sampleAENetwork` 的断网分支，B2-09 仅改 writeToNBT）。

---

## 一、本波修复 commit（5 项，均 `./gradlew build` BUILD SUCCESSFUL 后提交）

| # | commit | --stat | 条目 | 修复内容 |
|---|--------|--------|------|----------|
| 1 | `8074e07` | 1 文件，+63 | [SWN-B2-03] | 包 0 余额查询加持有校验：`WirelessEURequestQueue.drain` 在 playerNetServerHandler 判空后增加 `holdsMonitorFor`（主手 → Baubles 饰品栏 try/NoClassDefFoundError 降级 → 主背包 0-35，槽位口径对齐客户端 `WirelessMonitorHUD.scanMonitorInInventory`），仅当请求玩家实际持有绑定到包体 UUID 的 `PortableWirelessNetworkMonitor`（NBT OwnerUUID 引用物品侧 public 常量）才查询回发，否则静默丢弃。**修法与计划一行身份校验不同**：深查证实 WirelessMonitorHUD:181-207 支持合法持有他人终端并跨 owner 显示，身份校验（req.ownerUUID==player UUID）会打断该既定功能；持有校验语义 = 持有即授权，恰好封死仅凭枚举 UUID 无物品的泄露面 |
| 2 | `4b59d1e` | 1 文件，+13 | [SWN-B2-12] | 包 4 fromBytes 三 count 钳制（对齐包 6 `PacketSyncQuantumTerminalData:97` 范式）：sampleCount 钳 `AEMonitorWindowSeries.CAPACITY`=61（走势图 FIFO 容量），latestCount/avgCount 钳具名常量 `MAX_MONITOR_KEYS`=512（推导 = 2 × aeMaxMonitoredItems 上限 256，物品+流体）。S→C 自家服务器来源，纯健壮性，无协议变化 |
| 3 | `75d3628` | 1 文件，+33 | [SWN-B2-02] | AE 断网后信息屏无限期陈旧：`sampleAENetwork` 的 `!isAEConnected()` 早退分支改为 `pushAEMonitorOffline()`——同款 64D TargetPoint 推送 `PacketSyncAEMonitorData` 空数据（chartKey=null + 空集合），客户端 `receiveAEMonitorData` clear+addAll 语义即清显示。不复用 `sendAEMonitorDataToClients`（dataSet==null 短路 + dataSet 存在时推 WSD 旧值，均达不到清显示目的）。服务端 WSD 历史不动，重连自动恢复；节律 = 断网期间每 aeSampleInterval 一次空包，与在线推送同频。updateEntity 五段顺序不动 |
| 4 | `0138776` | 1 文件，+3/-1 | [SWN-B2-09] | 信息屏区块 NBT 双写 sync 数据：删除 `writeToNBT` 内 `writeSyncData(tag)` 调用（留注释指向 S35 专用）。安全性链实测：读侧 readSyncData 全 hasKey 守卫；OwnerName 由 writePlacementData 独立写入不受影响；cachedEu/cachedEut/cachedStatus/samples/aeChartSamples 断档后由 needsDataRefresh 冷启动从 WSD 正本重建；getDrops 走 writePlacementData 无关；S35 路径（getDescriptionPacket/onDataPacket 直接调 write/readSyncData）不受影响。存档瘦身键面：samples/aeChartSamples/cachedEu/cachedEut/cachedStatus 及 chartConfig/screen 等 S35 副本；旧档读取自然兼容 |
| 5 | `61eae1c` | 1 文件，+2/-18 | [SWN-B2-11] | 删除零消费死配置 `Config.aeMaxSamplePoints`（字段 + configuration.getInt 读块 + 类头 javadoc 更新）：grep 全仓实证仅 Config.java 定义/读取两处、零消费；实际容量由 `AEMonitorWindowSeries.CAPACITY` 固定 61。Forge 对配置文件残留键静默忽略，无兼容问题。真接线（CAPACITY 可配化）会动 8 窗口均值链级联 + 窗口 NBT 兼容，属结构改动，不实施 |

---

## 二、登记项（本波不动代码）

### B2-01 包 2/3 Netty 线程直改世界态【中】→ B07 已吸收闭合（commit `334362a`，见 §八）

- **登记决定**：本波不动代码，与 O2-B07（优化建议第二轮 :1071-1116，network↔tile 拆环）合并实施。**2026-08-22 更新：O2-B07 已按本节四步建议落地（B07 波 commit `334362a`，§八），drain 宿主取 (c) 自宿主队列，B2-01 随之闭合。**
- **深查结论备案**：包 2 `PacketUpdateNetworkInfoPanelConfig` Handler onMessage 直调 apply（距离/TE 类型/applyChartConfig 等），包 3 `PacketUpdateAETabState` switch 直调 setCurrentTab/setChartItem/toggleItemMonitor 等，均在 Netty 网络线程改 TE 态；与主线程 sampleAENetwork 的 for-each 并发（AEMonitorDataSet 17 张 HashMap）。包 3 Handler 缺包 2 同款 player null 守卫，队列化时顺手补齐。
- **拆环吸收建议（HEAD 实测版，供 O2-B07 实施时采纳）**：四步走——①纯新增 network/PanelActionQueue（仿 WirelessEURequestQueue ~70 行，ConcurrentLinkedQueue FIFO + **自有 @SubscribeEvent ServerTickEvent(END) 自宿主 drain**，无 PENDING_PLAYERS 去重——面板操作是异质变更序列，同 tick「清空→重绑」合法，去重会丢操作；drain 内主线程复验 playerNetServerHandler + 64D 距离 + TE instanceof 后逐字搬迁现有逻辑）；②包 2/3 Handler 缩为计数 + null 守卫 + 入队；③PanelBroadcastPort 接口拆 tile→network 边；④终态 network→tile 仅剩 drain 一处受控调用。
- **drain 宿主取舍**：推荐 (c) 队列自宿主（零新增跨包边、不触碰 QuantumControllerEventHandler，与 B2-05 完全解耦）；若 O2-B07 实施时改选 (a) QuantumControllerEventHandler 宿主，则与 B2-05 同文件必须串行。语义变化仅「配置应用延迟 ≤1 tick」，玩家无感。

### B2-10 getAEKey 无 NBT vs AE2 精确查询语义分裂【低】→ 降级（维持现状 + 文案登记）

- **登记决定**：key 升级不实施，维持现状。理由（实测证据）：key 字符串是 WSD 持久化键（AEMonitorDataSet 8 窗口 series 按 key 存取），改格式 = 旧序列孤儿化 + 需 clearAEData 迁移，直接顶撞「NBT 键逐字不动」硬约束；areItemStacksEqual→NBT 感知判等会改变存量 monitoredItems 的 toggle 语义（升级即行为漂移）。触发面窄（GT metaitem 多数无 NBT）。
- **文案登记动作（后置）**：GUI tooltip「按精确物品（含 NBT）匹配」说明（GuiNetworkInfoPanel + 新 lang 键，新键不违反 NBT 约束），建议搭 B2-15 lang 批次或 O2-B04-2 装配段窗口实施——本轮 TE 批不动 GUI。

---

## 三、批次③④⑤ + B2-05（另一波次 swn-editor-2 处置，本节由该波次追加）

- **输入依据**：`runs/r2-20260822/results/swn-survey-b` 深查报告（11 条目：9 可修 + 1 上抛 B2-17 + 1 处置项 B2-15）。
- **范围约束**：不触碰 PacketUpdateNetworkInfoPanelConfig / TileEntityNetworkInfoPanel 及批次①②登记包文件（本波 0 交叉）；B2-05 在确认批次①缩为 B2-03/12 包文件（不碰 QuantumControllerEventHandler）后直接修。
- **硬约束合规**：B2-14 五键（QT_Bound/QT_AnchorDim/X/Y/Z）字面量逐字不变（物品侧升 public static final + QuantumNetworkData 删冗余副本改引，编译期常量内联 → NBT 字节级零差异，旧存档终端直接兼容）；S35 键、updateEntity 五段顺序均未触碰。

### 三-1 本波修复 commit（9 项，均 `./gradlew build` BUILD SUCCESSFUL 后提交）

| # | commit | 条目 | 修复内容 |
|---|--------|------|----------|
| 1 | `6f7cf03` | [SWN-B2-04] | WirelessMonitorHUD owner/hudMode 复合变化分支增加 ownerChanged 判定：换绑定账户时 `dataSet.clear()` + `syncedEuStr=null`（旧 owner 网络快照与 EU 显示残留不再跨账户泄漏，切换瞬间不再短暂显示旧 owner 余额）；仅 HUD 模式变化（同 owner）不清，与世界切换保留 dataSet 语义（用户确认）不冲突 |
| 2 | `d8c2c96` | [SWN-B2-08] | PortableWirelessNetworkMonitor.bindOwner（2 条）与 updateOwner（1 条）的 addChatMessage 加 `!isRemote` 服务端门（与 TestCoin 范式对齐），消除 onItemRightClick 双端镜像导致的提示双发 |
| 3 | `7191c1e` | [SWN-B2-14] | QT_* 五键单源化：物品侧 5 常量升 public static final，QuantumNetworkData 删 5 行冗余副本（含自认纪律产物注释）改引物品侧常量；全仓 "QT_" 字面量恰剩物品侧 6 处定义。编译期常量内联 → NBT 字节级零差异。= O2-B09 第①步 |
| 4 | `f422aca` | [SWN-B2-16] | 删除 MonitorScanResult.stack 零消费死字段（唯一调用方 :181-186 只读 ownerUUID/hudMode），NONE 与 inspectMonitorStack 构造点同步收紧，javadoc 同步。纯死码删除零行为变更 |
| 5 | `2a0aafb` | [SWN-B2-13] | QuantumControllerRegistry.getAll() 复用快照冻结 `Collections.unmodifiableSet(new HashSet<>(controllers))`（补 import java.util.Collections），与 QuantumNetworkStatsCache/QuantumNetworkData 缓存口径对齐；唯一调用方 sweepWorld 只读遍历零改动，复用窗口误写由静默污染变立即抛异常 |
| 6 | `7c64d8f` | [SWN-B2-05] | onPlayerInteract 拦截提示冷却写入从玩家所在世界 `event.world.getTotalWorldTime()` 改为主世界时钟 `MinecraftServer.getServer().worldServerForDimension(0)`（服务端门内非空安全，与 onServerTick 清理基准 :347/:351/:357 同域），修复跨维度冷却缩水（下界 ~1s → 2s），主世界语义不变。含 spotless 折行修正后 amend |
| 7 | `12ec4a9` | [SWN-B2-06] | GTswn_Cover_DynamoWireless 缓冲池 `storedEU += euToTake` 改饱和累加 `safeAdd`（复刻 MTEWirelessEnergyMonitor.safeAddStep / SWN-BUG-04 同式私有静态 helper），防名义电容 ~90 天理论溢出回绕；:129 double 损耗舍入维持观察（不可达量级） |
| 8 | `66720f5` | [SWN-B2-07] | GTTierUtil 安培 (int) 强转回绕修复（纯显示层）：getGTTier amperage 改 double 局部量（仅参与 >4 比较，删 3 处强转）；formatGTPower 过载判定用未截断 amp 先行、显示值 `Math.min(amp, 999.0)` 封顶 999A。阈值 4.6×10^18 EU/t |
| 9 | `9fa5b60` | [SWN-B2-15] | zh_CN.lang 全角冒号半角统一：深查核实口径 **38 行**（登记 10 行为子集），`sed 's/：/:/g'` 后实测余 0、git diff 恰 38 行、UTF-8 编码不变，与 en_US 半角口径对齐。**口径说明**：深查原建议为 (a) 零改动+头注释，本波按主代理派发指令字面「修 38 行全角冒号」执行 (b) 半角统一（纯排版无功能差异）；如需回退 (a) 单 commit revert 即可 |

### 三-2 上抛裁决（本波不动代码）

- **B2-17 assemble 不校验量子化状态【低｜语义确认项】**：QuantumNetworkData.assemble 离线判定链（维度/区块/TileController/GridAccess）确无 isQuantized，与节点侧 TileEntityNetworkQuantumNode.tryConnect 严格查 isQuantized（ANCHOR_NOT_QUANTIZED）口径分叉成立。若统一口径，修法已备：assemble 步骤 1 后（:241 与 :243 之间）插 `if (!QuantumControllerRegistry.get(world).isQuantized(anchorX, anchorY, anchorZ)) return data;` 一行离线镜像。裁决点：终端绑定语义是「网络」还是「量子化网络」（数据本身仍真实），产品决策归主代理。

### 三-3 验证与核对

- 本波 9 个 commit 均在提交前 `./gradlew build` BUILD SUCCESSFUL（jabel：modern 语法 / Java 8 字节码，全部修法 Java≤8 stdlib、零新依赖）；B2-05 首提因 spotless 行长折行 FAILED，修正后 amend（7c64d8f）再验全绿——期间无坏 commit 残留。
- 每个 commit 前 `git status --short` 自审 + 显式 `git add` 单条目文件提交；并行波次工作区内的 TileEntityNetworkInfoPanel 中间态全程未纳入本波 add。
- B2-15 编码核验：处理前后 `file` 均为 UTF-8 text，仅 38 行冒号字符变化。
- 人工冒烟待办（后续进游戏执行）：HUD 开启下换绑他人终端观察 EU/t 跳变/残留（B2-04）；未绑定终端首次右击 2 键、Shift+右击重绑 1 键各一组提示（B2-08）；旧存档量子终端绑定读取正常（B2-14）；下界对已量子化控制器按住右键拦截提示 2s 间隔（B2-05）；游戏内中文文案冒号显示正常（B2-15）。

---

## 四、验证与核对

- 本波 5 个 commit 均在提交前 `./gradlew build` BUILD SUCCESSFUL（jabel：modern 语法 / Java 8 字节码，全部修法 Java≤8 stdlib、零新依赖、零新 stdlib）。
- 每个 commit 前经 `git status --porcelain` 自审 + 显式 `git add` 单文件提交，与并行波次（量子网络批次）零文件交叉。
- B2-11 编译产物核验：`build/classes/java/main/.../Config.class` 晚于源文件且字节码中 `aeMaxSamplePoints` 字符串已消失。
- 人工冒烟待办（后续进游戏执行）：持他人终端 HUD 正常显示（B2-03 跨 owner 合法路径不误伤）；无终端时伪造 UUID 请求无回包；AE 断网后信息屏走势图/监控列表清空、重连后恢复（B2-02）；旧存档信息屏加载后 EU/走势冷启动刷新正常（B2-09）；配置文件残留 aeMaxSamplePoints 键被静默忽略（B2-11）。

## 五、OPT P0 快赢批 + 网络优化批（波次 swn-opt-editor 处置，本节由该波次追加）

- **输入依据**：`runs/r2-20260822/results/swn-survey-b` OPT 部分（P0 七条锚点快核 + O2-17 完整深查）+ `plan/全局调查-优化建议-第二轮.md` 登记条目。
- **范围**：P0 七条 O2-25/26/27/28/29/30/11 + 网络批 O2-17/18/20/21，共 11 项全部处置；**O2-19 随 O2-17 落地按登记原判跳过**（30B×2/s 已无足轻重，10t 保留更跟手）；**O2-24 维持登记等待人工实测（不实施）**；**O2-22/23 押后**（登记建议排 O2-04 后）；**E 线（TileEntityNetworkInfoPanel 四域拆分）/B 线（B09→B07→B08/B10）留后续波**——本轮不碰 PanelActionQueue 拆环，B07 吸收 B2-01 维持登记。
- **硬约束合规**：NBT/S35 键逐字不动（O2-28 仅加内存缓存与 store revision 计数，序列化面零触碰；O2-20 只改包 4 线上格式，AEMonitorSample 的 WSD/NBT 与聚合链 timeMs 不受影响）；updateEntity 五段顺序未触碰；jabel 全程 modern 语法/Java 8 字节码/零新依赖（每次提交前 `./gradlew spotlessApply build` 全绿）。
- **批内排序修正**：O2-29 先于 O2-28 落（同文件 TEPanel，先删死访问器避免删行致锚点漂移，survey 快核建议）；O2-25→O2-26 串行（共享双 lang 追加面）。

### 五-1 本波 commit（11 项，均 `./gradlew build` BUILD SUCCESSFUL 后提交）

| # | commit | 条目 | 处置内容 |
|---|--------|------|----------|
| 1 | `e2ac30a` | [SWN-O2-25] | CommandGTSWN 15 处硬编码聊天消息迁移 `gtswn.command.*` lang 键（transfer 7 + cleanup 7 + join 英文遗留 1，双语补齐 15 键）；join.usage 的 RED 色码内嵌 lang 值（显示不变）。英文客户端 OP 反馈不再中英混杂 |
| 2 | `46d3f31` | [SWN-O2-26] | 量子节点过载倒计时 4 处时长参数改 lang 键（`gtswn.chat.quantum.duration.3min/2min/1min/10s` 双语）；**顺修连带缺陷**：overload_warning 值 `{0}`→`%s` 对齐 String.format（messagePlacer 走 translateToLocalFormatted，原 `{0}` 不被代入、客户端显示字面 "{0}"） |
| 3 | `ad81e81` | [SWN-O2-27] | HUD 渲染路径免每帧 UUID.fromString：`parsedOwnerUuid` 缓存随 cachedOwnerUUID 变更点统一失效/重建（`setCachedOwnerUUID` 单点，setEnabled/clearCache/扫描变更/失去监视器四处收敛）；60fps 下每秒 60 次字符串解析归零，非法 UUID 语义与原 try-catch return 一致 |
| 4 | `17f8cfe` | [SWN-O2-29] | 删 TEPanel 4 个零调用死访问器（getCachedEut/getTrackingWindow/setAETrackingWindow/prevTrackingWindow，全仓 grep 实证仅声明行），-37 行纯删除零行为变更；四域拆分前减搬迁量 |
| 5 | `f3cdbeb` | [SWN-O2-28] | getOwnerDataSet 每 tick 重解析缓存：TE 缓存 store/数据集引用 + **store revision 校验**（NetworkInfoDataStore.remove/cleanupStale 实际移除条目时 +1，缓存据此失效重新 getOrCreate——cleanup_info_data 命令清理后从空数据集重启的语义不变，优于登记的纯引用缓存方案）；bindOwner/readPlacementData 失效点；每屏每 tick 免 2 次 UUID.toString+mapStorage 查 |
| 6 | `193aa0f` | [SWN-O2-30] | 删数据层/渲染层 8 个零调用死方法（NetworkInfoDataSet.isSilent/getRecentEUT/windowToMillis + 9 个仅供其使用的 *_MS 常量、EUDataSet.getNewest/getOldest/isFull、RenderNetworkInfoPanel.sci、QuantumNetworkData.computeUsedChannels）并修正 TEQNode :549 javadoc 漂移（实际经统计快照内联自算），净 -125 行 |
| 7 | `3352a9d` | [SWN-O2-11] | 红石模式判定收敛 `computeRedstoneOutput(mode, eu, p1, p2, currentOutput)` 纯函数：getGeneralRS/updateRedstoneOutput/loadNBTData 三处同构 switch 单源化，NBT 加载站点传 false 维持现状语义（**O2-12 滞后带重载闪断修正另行独立 commit，本波不实施**）；零行为变更净 -37 行，顺删 getGeneralRS 死 null 守卫、修 anchorMode 注释缺模式 2 与站点 2 保持条件注释错位 |
| 8 | `f0c67c7` | [SWN-O2-17] | 量子终端轮询改 **disc 7 短回包**：新类 PacketSyncQuantumTerminalDataLite 仅序列化 GUI 消费 9 字段（online+锚点四维标+used/totalChannels+quantumNodeCount+channelsInfinite，固定 30B），CommonProxy 空实现/ClientProxy 覆写（复刻 v1.5.14 类加载安全模式），QuantumTerminalRequestQueue 正常 :78/异常兜底 :89 两处 sendTo 换发（sendTo 实测 2 处口径），recordTerminalReply 审计不动；包 6 保留作协议回退位与 GUI 回扩位；同 jar 双端无版本偏斜窗口 |
| 9 | `711873b` | [SWN-O2-18] | 量子终端装配端 Config 门控跳过死负载枚举：`quantumTerminalAssembleFullData`（ae2 类目，默认 false）跳过能量四项/存储字节/设备列表枚举（每 100t 每锚点免一次 AE2 全枚举），代码保留不物理删除——审计模式与未来 GUI 回扩不受影响；缓存/统计快照（GUI 消费的 9 字段源）不受门控影响 |
| 10 | `85850ed` | [SWN-O2-20] | 信息屏广播第一刀：样本 timeMs 不上线（全 client/ grep 零消费实证，32→24B/样本，客户端重建以 0 占位）+ monitorLatest/monitorAvg300s **双 Map 合一上线**（同 key 单写，1B flags bit0=hasLatest/bit1=hasAvg 标记段有无）+ 无绑定空屏周期推送门控（chartItem/chartFluid/监控列表全空时不再每 100t 推空包；**解绑/清空/移除路径补即时推送**保住 receiveAEMonitorData clear+addAll 的清显示语义）+ 删零调用 5 参构造器 |
| 11 | `b66222d` | [SWN-O2-21] | sendAEMonitorDataToClients 入口 64 格接收者预检（`hasNearbyReceiver`，平方距离比较，只查本维度 playerEntities 与 TargetPoint 半径一致）：无人区常载屏每采样间隔免 build 走势 61 点+双 Map 三段集合再发空集；视锥级过滤不实施 |

### 五-2 O2-17 回包量前后对比估算（验收项）

- **包 6 全量（旧）**：固定段 119B = 1(online)+16(锚点四维标)+8(used+total)+80(十 double)+1(powerInfinite)+4(totalMachines)+4(entryCount)+4(quantumNodeCount)+1(channelsInfinite)，另加条目段 ~10B×N（ItemStack 序列化 ~6B + 4B count）。
- **包 7 短包（新）**：固定 30B = 1+16+4+4+4+1，无条目段。
- **对比**：N=0 时 119→30B（**-74.8%**）；N=128 满配 ~1,399→30B（**-97.9%**）——与登记「全区间 75%~98%」口径吻合。每开 GUI 玩家下行 2 回包/s 同比例降（N=0：238→60B/s；满配 ~2.8KB→60B/s）。
- **请求侧**：包 5 无字段不变；轮询间隔 10t 维持（O2-19 登记原判跳过）。
- **O2-20 满配包体估算**：走势段 61×32=1,952→61×24=1,464B（-25%）；监控段 64 key 双 Map 合一省 ~64×(key 字符串 ~20B+4B count-1B flags)≈1.5KB；合计约 **-30%**（8.3→~5.8KB 口径与登记一致）。

### 五-3 验证与核对

- 本波 11 个 commit 均在提交前 `./gradlew spotlessApply build` BUILD SUCCESSFUL（spotless 为既定格式门）；波末全量重编译复验 BUILD SUCCESSFUL，jar 产物 `gtswn-v1.6.27-fix-20260822-r2.27+b66222d3b8-dev.jar` 落盘。期间一次 `clean build` 失败为 Windows 文件锁（build/rfg/recompiled_minecraft-1.7.10.jar 被进程占用，`clean` 任务无法删目录），与代码无关，随后非 clean 全量构建复验通过。
- 每个 commit 前 `git status --short` 自审 + 显式 `git add` 单条目文件提交；工作树收尾 clean。
- 死代码删除（O2-29/30 及 O2-20 五参构造器）均先全仓 grep 实证零调用再删；活跃近邻（getAETrackingWindow/getWindowName/nextTrackingWindow/isAllSameValue/calculateRecentEUT）逐一区分勿伤。
- 包 4 线上格式变更（timeMs 裁剪+双 Map 合一）与 disc 7 新增均要求同 jar 双端部署（与既有协议演进惯例一致）。

### 五-4 待后续波 / 登记

- **E 线**（O2-01~04 四域拆分，E0=O2-29 已先落）、**B 线**（B09 余步→B07 拆环吸收 B2-01→B08/B10）、**O2-22/23**（广播裁剪第二刀，登记建议排 O2-04 后）、**O2-12**（红石滞后带重载闪断行为修正，独立 commit）、**O2-24**（阻塞于人工实测六用例）、**wiki 3 词条过时 + packet-channel-registry.md disc 7 顺延登记**（GTNHwiki-Edit 流程）。
- 人工冒烟待办（进游戏执行）：/gtswn transfer/cleanup_info_data 双语显示（O2-25）；量子节点超限倒计时 4 档警告时长代入双语（O2-26）；HUD 换绑/失去监视器行为不变（O2-27）；cleanup_info_data 命令后信息屏冷启动重启（O2-28）；红石 5 模式边界表（O2-11 零行为核验）；128 条满配开量子终端 GUI 抓包 ≤64B 负载 + 正常/离线/异常三条回包路径（O2-17）；信息屏走势/监控正常显示、解绑/移除即时清空（O2-20）、无人区停止推送走近恢复（O2-21）。
---

## 六、数据集存储线 + R4 提前批（波次 swn-opt-editor2 处置，本节由该波次追加）

- **输入依据**：`runs/r2-20260822/results/swn-survey-b`（锚点快核与 E/B 拓扑）+ `plan/全局调查-优化建议-第二轮.md` §3.4/§3.5/§3.6/§2.1 与附篇 A（O2-A10 提级方案、批次表 5.1 R4 行"无前置可提前穿插"）。
- **范围**：数据集存储线 4 条（O2-13→O2-14、O2-15→O2-16）+ R4=O2-07 提前批 + O2-19 处置，共 6 项全部处置；**本轮零触碰 TileEntityNetworkInfoPanel**（E 线拆分与 PanelActionQueue 拆环不碰，updateEntity 五段顺序天然不受影响）。
- **O2-19 处置（不实施，维持跳过判定）**：深查建议"与 O2-17 同批排期时可直接跳过（30 字节×2/s 已无足轻重，10t 保留更跟手）"；O2-17 已于本 run 前波落地（`f0c67c7`），跳过条件成立，轮询间隔维持 10t，无代码变更。
- **硬约束合规**：NBT/S35 键逐字不动——O2-13/14 两步重构后 series/counter 键名、v1.5.17 hasKey 守卫、键写入顺序（series5m→1Y、counter5m→3M、lastSampleTick）与 EU 侧"无条件写全 8 series 键"/AE 侧"未触碰窗口不写键"的序列化语义全部逐字保留；O2-15 的 `sets`/`id`/`data` 字节级不变；O2-16 异常策略整体留子类（终端离线快照兜底 v1.6.1-4b 防卡死契约、EU 静默丢弃均不动）；jabel 全程 modern 语法/Java 8 字节码/零新依赖（Supplier/IntConsumer/Function 均为 Java 8 stdlib）。

### 六-1 本波 commit（5 项，均 `./gradlew spotlessApply build` BUILD SUCCESSFUL 后提交）

| # | commit | 条目 | 处置内容 |
|---|--------|------|----------|
| 1 | `73463ea` | [SWN-O2-13] | AEMonitorDataSet 窗口结构数组化：WINDOW_DEFS[8] 单点定义 series/counter NBT 键与流入阈值（12/8/3/7/4/3/4），16 张并行 Map（8 series+7 counter）改按下标循环访问，addSample 8 级嵌套 if 链（93 行）循环化为 source 沿链传递的单循环；净 -128 行（480→352） |
| 2 | `fa990d0` | [SWN-O2-14] | 新增 WindowChain<S> 泛型骨架（275 行）单源承载 8 窗口 61 点 FIFO、流入计数链与窗口 NBT 编解码；EU 薄壳 NetworkInfoDataSet 直接继承（222 行，eager 窗口保无条件写全 8 series 键），AE 薄壳 Map<String,Chain> 单维化（264 行含 Chain 内嵌，懒创建保 if(s!=null) 不写空键语义）；删除 NetworkInfoWindowSeries/AEMonitorWindowSeries 两份 123 行类型替换拷贝；样本编解码/派生构造/聚合字段（eut/rate）走子类钩子，采样锁语义差异（EU 数据集级 vs AE per-key）留外壳；四文件 1195→761 行（登记口径 ~1253 基于 O2-30 删除前行数，比例吻合） |
| 3 | `a115baa` | [SWN-O2-15] | 新增 SavedDataUtil.loadOrCreate 模板（三份逐字符相同的 get()——Registry/NetworkInfoDataStore/AEMonitorDataStore——收敛为 1 行委托，Registry 保留 dimensionId 赋值一行）+ AbstractDataSetStore<T> 骨架（两 panel store 的 dataSets/getOrCreate/remove/NBT 读写单源化，"sets"/"id"/"data" 字节级不变）；子类仅留活跃判定/过期清理/revision/仅查询自有方法；16 处调用面签名不变零波及 |
| 4 | `3625f4b` | [SWN-O2-16] | 新增 PlayerRequestQueue<P> 泛型基类单源承载「Netty 入队→主线程 drain」骨架（待处理队列+同玩家去重门+排空循环，全仓 2 处 ConcurrentLinkedQueue 合一）；量子终端/EU 两队列改继承+静态门面单例（基类方法命名 offer/drainAll 避开与子类静态门面 enqueue/drain 的同名隐藏冲突），入队审计走 onEnqueued 钩子，异常策略整体留子类 process；排空点 2 处调用签名不变 |
| 5 | `07f7e2d` | [SWN-O2-07] | GuiAEMonitorList 285 行内嵌类提级为顶级包私有类（R4 提前批，批次表"无前置可提前穿插"）：8 项隐式捕获显式化（left/top/xSize 构造快照、monitoredEntries 改 Supplier 活读、sendMonitorToggle 改 IntConsumer、panel 构造注入、宿主访问器 font()/client()/fillRect() 承载 GuiScreen 跨包 protected 字段与 Gui.drawRect 转发——登记两选项之一"或经 host"，避免在列表类复制 vanilla GL 代码；drawItemIcon/drawFluidIcon 改包私有）；格式化三件套（formatAEMonitorAmount/Rate、rateColor）随迁列表类（O2-A07 ClientChartFormat 未落地，落地后改引）；滚轮优先于 super/列表区点击一律消费/mouseMovedOrUp 无条件直调三不变量与 action 3/4 清除回包语义不变；宿主 1333→1026 行 |
| — | （无） | O2-19 | 不实施：O2-17 短回包已落地，30B×2/s 已无足轻重，10t 保留更跟手（深查原判），POLL_INTERVAL_TICKS 维持 10 |

### 六-2 锚点核对与波内修正

- 锚点基本无漂移：AEMonitorDataSet 480 行、GuiNetworkInfoPanel 1333 行、两 WindowSeries 各 123 行与登记一致；NetworkInfoDataSet 469（O2-30 删 -41）、NetworkInfoDataStore 170（O2-28 revision +15）、WirelessEURequestQueue 126（B2-03 持有校验 +63）为上波已解释漂移，区域判定不变。
- O2-14 实施形态按批次表 5.1 第 12 行"EU 半边先迁、AE 半边套壳"整体落地（原判"并入 E3-E5 窗口"为排期偏好而非技术依赖，存储线整体先行后 E3-E5 拆域直接在 WindowChain 薄壳之上进行，避免二次返工）；O2-13 的 WINDOW_DEFS 结构被 O2-14 吸收进 WindowChain 的 SERIES_NBT_KEYS/COUNTER_NBT_KEYS/INFLOW_THRESHOLDS 单点，两步各自独立可 build。
- AE per-key 结构从"8 series Map+7 counter Map"收敛为 Map<String,Chain> 后，getKeys()/toNBT() 的"以 5m 集为准"口径（原 series5m.keySet()）以 chain.hasWindow(WINDOW_5_MIN) 等价保持——readFromNBT 未带 series5m 键的 entry 不计入、重写时照旧丢弃的边缘行为逐字一致。

### 六-3 验证与核对

- 本波 5 个 commit 均在提交前 `./gradlew spotlessApply build` BUILD SUCCESSFUL；波末全量重编译复验 BUILD SUCCESSFUL，jar 产物 `gtswn-v1.6.27-fix-20260822-r2.33+07f7e2d004-dev.jar` 落盘。
- 每个 commit 前 `git status --short` 自审 + 显式 `git add` 单条目文件提交；工作树收尾 clean（本条目段随后单独 docs commit）。
- O2-16 两次编译失败自修：Request 内嵌类 private 可见性不可出现在 extends 泛型实参（改包私有）；子类静态门面 drain 与基类实例方法同名隐藏冲突（基类改名 offer/drainAll）——均为编译期问题，无坏 commit 残留（提交前修正）。
- NBT golden 回环（4 窗旧档+8 窗新档 读→写→读）为登记验收项，静态核对已完成（键名/守卫/顺序/缺省值逐字对齐），进游戏回环并入人工冒烟。

### 六-4 待后续波 / 登记

- **E 线**（E1-E6 信息屏四域拆分，E0=O2-29 已先落；E3 PanelConfigStore 为串行化关键批）、**B 线**（B09 余步→B07 PanelActionQueue 拆环吸收 B2-01→B08/B10）、**R1/R2/R3**（ClientChartFormat→ChartRenderer 双批）、**R5**（AbstractPanelTab 三页成类，O2-07 提级后 Gui 本体已降至 1026 行）、**O2-22/23**（广播裁剪第二刀，排 O2-04 后）、**O2-12**、**O2-24**、**wiki 顺延登记**（含本轮 GuiAEMonitorList 提级对 `ui/network-info-panel-gui.md` file:line 的影响）——均维持登记待后续波。
- 人工冒烟待办（进游戏执行）：旧存档信息屏 EU 走势/AE 走势/AE 监控数据加载与继续采样正常（O2-13/14/15 序列化回环，重点 4 窗旧档升级）；信息屏重启后 EU 数据集多屏共享与 cleanup_info_data 清理语义不变（O2-15/28 链路）；AE 监控列表滚轮滚动/滚动条拖拽/清除按钮移除回包（action 3/4）/条目数变化后滚动校正（O2-07）；量子终端 GUI 轮询回包与掉线兜底（O2-16 队列重构零语义变化核验）；无线 EU 查询 HUD 正常（O2-16 EU 侧）。

---

## 七、E/B 线起点批核验（波次 swn-opt-editor3 处置，本节由该波次追加）

- **输入依据**：`runs/r2-20260822/results/swn-survey-b`（E/B 线拓扑确认）+ `plan/全局调查-优化建议-第二轮.md` 附篇 A 批次表 E0 行与附篇 B O2-B09。
- **派发范围**：E0（E 线起点）+ B09（B 线起点）两个起点批；E1-E6/R1/R2/R3/R5/B07/B08/B10/O2-22/23 留后续波登记。
- **核心结论：两个起点批在派发基线 HEAD `4af7bda` 已由前波落地**（`git merge-base --is-ancestor` 实证均为 HEAD 祖先），本轮零代码改动、转核实与登记，不产生重复实现 commit：
  - **E0 = O2-29** → 前波 commit `17f8cfe`（§五-1 #4，OPT P0 快赢批）：4 死访问器（getCachedEut/getTrackingWindow/setAETrackingWindow/prevTrackingWindow）当前 HEAD 全仓 grep 零命中；蓝图「活跃勿动」约束核对通过——`getAETrackingWindow`（声明现 :1017，活跃调用 :543/:548）与 `getWindowName`（声明现 :1371，Gui :179/:495 + Render :971/:1058 共 4 处活跃调用）健在未伤；实际 -37 行（批次表预估 -45，以实测为准）。
  - **B09 起点步 = B2-14 修复形态** → 前波 commit `7191c1e`（§三-1 #3，批次③④⑤波）：QT_ 字面量全仓恰剩物品侧 6 处定义（5 键升 public static final + NBT_BOUND_NAME 本就物品侧独有维持 private）；QuantumNetworkData 冗余副本段清零（键名段仅存单源化注释），原引用点全部改引 `ItemNetworkQuantumTerminal.NBT_*`，diff 内五键字面量逐字不变（编译期常量内联 → 旧存档终端 NBT 字节级零差异）。**B09 完整形态**（TerminalBinding 类 + EventHandler :133 instanceof 门控去除 + offlineFromStack 改引）维持登记待后续波，本轮不扩范围。
- **构建核验**：HEAD `4af7bda` 工作树 clean 状态下 `./gradlew build` **BUILD SUCCESSFUL**（25 任务，checkstyleMain/jar/reobfJar 全过）——「E0+B09 落地、build 全绿」验收成立。
- **硬约束合规**：本轮零源码改动，NBT/S35 键与 updateEntity 五段顺序天然未触碰（核验基线即前波合规结论）。
- **待后续轮登记（批次表就绪）**：E1（ScreenStructureUtil 五工具收敛）→E2（ScreenStructure 域）→E3（PanelConfigStore，串行化关键批）→E4（EUCacheBridge+窗口枚举化，O2-28 已先行）→E5（AEMonitorController）→E6（门面二阶段，可并入 R5）；B07（PanelActionQueue 拆环吸收 B2-01，§二拆环四步建议已备）→B08/B10（随触碰窗口穿插）；R1/R2/R3/R5（渲染/GUI 线，O2-07 提级批已先行）；O2-22/23（广播裁剪第二刀，登记建议排 E5 后）——均**批次表就绪待后续轮**；**O2-24 维持等人工实测**（六用例单人+dedicated 各一轮，不实施）；O2-12 独立 commit、wiki 顺延登记（含 disc 7 与 GuiAEMonitorList 提级词条）维持既有登记。
- **主代理对账提示**：E/B 线起点批已闭合，下一可执行批为 E1（E 线）与 B07（B 线），后续派发可直接从 E1/B07 起排。

---

## 八、E1 + B07 实施批（波次 swn-opt-editor4 处置，本节由该波次追加）

- **输入依据**：`runs/r2-20260822/results/swn-survey-a`（B2-01 深查：自宿主队列方案 §1/§8）+ `runs/r2-20260822/results/swn-survey-b`（E 线拓扑与批次表锚点）+ `runs/r2-20260822/results/swn-opt-editor3`（起点批核验：E0/B09 已闭合，下一可执行批 E1/B07）+ `plan/全局调查-优化建议-第二轮.md` 附篇 A 批次表 E1 行与附篇 B O2-B07。
- **派发范围**：E1（E 线第二步 = O2-01a）+ B07（B 线拆环，吸收 B2-01——按 swn-survey-a 方案 (c) 自宿主队列）；**E2-E6/R1-R3/R5/B08/B10/O2-22/23 留后续波登记**。
- **基线**：`f9213cc`（工作树 clean 实测）→ 本波 2 个代码 commit 推进至 `334362a`。

### 八-1 本波 commit（2 项，均 `./gradlew spotlessApply build` BUILD SUCCESSFUL 后提交）

| # | commit | --stat | 条目 | 处置内容 |
|---|--------|--------|------|----------|
| 1 | `30365fb` | 3 文件，+141/-205（净 -64） | [SWN-E1] | 新增 `common/block/ScreenStructureUtil`（124 行，五工具 getHorizontalFacingFromEntity/normalizeFacing/getEdgeMask/isCompatibleScreenPart/opposite 方法体逐字搬迁零行为变更），`BlockNetworkInfoPanel` 与 `BlockNetworkInfoPanelExtender` 删双份私有副本（两文件 -205/+17）改引同包工具类；`findNeighborFacing`（拓展屏独占）首轮不迁留原类（javadoc 标注）；两类 MathHelper 死 import 清除。净 -64（批次表预估 -85，差额为工具类 javadoc 头——双份删除量 188 行与登记 ≈182 行吻合，以实测为准） |
| 2 | `334362a` | 8 文件，+327/-90 | [SWN-B07] | **network↔tile 拆环 + 吸收 B2-01 闭合**：① 新增 `network/PanelActionQueue`（ConcurrentLinkedQueue FIFO + 自有 @SubscribeEvent ServerTickEvent(END) 自宿主 drain，CommonProxy.preInit 与通道注册同址 `register()`；**无同玩家去重门**（不继承 PlayerRequestQueue——面板操作是异质变更序列，同 tick「清空→重绑」合法，去重丢操作）；drain 逐条复验 playerNetServerHandler 非空 + 64D 距离 + TE instanceof 后执行逐字搬迁的包 2 apply/包 3 switch，单条 Throwable 记日志丢弃不中断后续）；② 包 2/3 Handler 缩为计数 + player null 守卫（包 3 补齐 B2-01 登记的缺失）+ 8 格距离快筛 + 入队，删 Handler 内 apply 私有方法；③ 新增 `common/panel/PanelBroadcastPort` 接口 + `network/NetworkPanelBroadcastPort` 实现（CommonProxy.init 一行注入），TE 两处推送（sendAEMonitorDataToClients + B2-02 pushAEMonitorOffline 空推送）改走端口（包体构造与 TargetPoint 逐字搬迁），**tile→network import 归零**；P4 javadoc `{@link}` 改 `{@code}` 删 import——network→tile 编译面仅剩 PanelActionQueue 一处受控调用（survey §8 终态达成） |

### 八-2 硬约束合规与设计取舍

- **NBT/S35 键逐字不动**：E1 零序列化面（纯方块工具）；B07 触碰的 TE 仅两处推送调用与 import/静态端口字段，writeToNBT/readFromNBT/writeSyncData/readSyncData/getDescriptionPacket/onDataPacket 一字未动；包 2/3 的 toBytes/fromBytes 与 action 常量未动，线上协议零变化。
- **updateEntity 五段顺序不可乱**：E1 不触 TE；B07 改的是第 5 段被调推送方法的出口（sendAEMonitorDataToClients 尾部 sendToAllAround→端口调用）与网络包 Handler 线程归属，五段结构与顺序逐字未动。
- **drain 宿主取舍**：按 swn-survey-a 方案 (c) 队列自宿主——不挂 QuantumControllerEventHandler（避免与 B2-05 同文件串行、面板操作挂量子域语义不当）也不挂 NetworkInfoMonitorScheduler（panel→network 会成新环），零新增跨包边（新增边仅 main 组合根 CommonProxy→network，与既有方向一致）。
- **语义变化**：仅「面板配置应用延迟 ≤1 tick」（O2-B07 已评估玩家无感）；同玩家连点操作 FIFO 保到达序（单连接单 Netty 线程入队）；异常策略比旧路径更稳（旧 Netty 直改异常会走 netty 管道踢出玩家，新路径记 error 日志丢弃该条、继续后续操作）。
- **jabel 合规**：全部 Java≤8 stdlib（ConcurrentLinkedQueue/Throwable 捕获），零新依赖零新 stdlib；modern 语法编译 Java 8 字节码。

### 八-3 验证与核对

- 两个 commit 均提交前 `./gradlew spotlessApply build` BUILD SUCCESSFUL（含 checkstyle/jar/reobf）；波末收尾 `git status` clean。
- 拆环实证：`grep -rln "import com.miaokatze.gtswn.common.tile" network/` 仅剩 PanelActionQueue；`grep -rn "gtswn.network" common/tile|panel|block` 仅剩 lang 键/纹理路径字符串字面量与一处注释文本，无 Java 引用。
- E1 零行为核对：五工具方法体与两份原拷贝逐字一致（主屏侧 isCompatibleScreenPart 的全限定 Extender 引用统一为 import 短名，同类等价）；调用点 5 处 + findNeighborFacing 内 1 处全部改引。
- 人工冒烟待办（后续进游戏执行）：方块放置朝向与拓展屏 L 形/多向连接贴图边缘掩码正常、破坏后定向重建（E1）；面板连点切页/绑定/清空监控无双丢或乱序（B07 FIFO）、EU/AE 图表配置与 action 按钮即时生效（≤1 tick）、AE 走势/监控推送正常、断网空推送清显示（B2-02 路径经端口）且重连恢复（B07）。

### 八-4 待后续轮 / 登记（批次表就绪待后续轮）

- **E 线**：E2（ScreenStructure 域迁入，E1 工具类届时收编）→E3（PanelConfigStore，串行化关键批）→E4（EUCacheBridge+窗口枚举化，O2-28 已先行）→E5（AEMonitorController）→E6（门面二阶段，可并入 R5）；**R1/R2/R3/R5**（渲染/GUI 线）；**B 线**：B08（tile↔quantum 拆环）→B10（items↔hud 拆环，随触碰窗口穿插），B09 完整形态（TerminalBinding）维持登记；**O2-22/23**（广播裁剪第二刀，现可落 PanelBroadcastPort 实现内）；O2-12、O2-24（等人工实测）、wiki 顺延登记（disc 7 + GuiAEMonitorList 提级 + 本轮 E1/B07 对相关词条的 file:line 影响）维持既有登记。
- B2-01 状态由「登记转并入」改「**B07 已吸收闭合**」（§二已同步更新）。

---

## 九、E 线四域拆分 E2-E5 实施批（波次 wave8-swn-e 处置，本节由该波次追加；E2 为上一执行者提交、本波补记）

### 九-1 本波 commit（4 项，均 `./gradlew spotlessApply build` BUILD SUCCESSFUL 后提交）

| # | commit | --stat | 条目 | 内容 |
|---|--------|--------|------|------|
| 0（补记） | `b32d284` | 5 文件，+382/-266 | [SWN-E2] | ScreenStructure 结构域迁入 common.tile.screen（rebuildScreen/findLargestFilledRect/renderBounds/detach/isCompatibleScreenPart/addPlaneNeighbors/rebuildNearbyScreens/key+parseKey 逐字搬迁，TE -266 新类 339），收编 E1 工具类 ScreenStructureUtil 至同包，TE 留 4 门面；screen/screenInitialized 随域平移、`screen` 键逐字不动；五段顺序与两侧 isCompatibleScreenPart 口径差异保持（上一执行者提交，波内核验） |
| 1 | `dfb3fe3` | 2 文件，+774/-460 | [SWN-E3] | PanelConfigStore 配置域迁入（EU 三组+AE 三组配置字段、applyChartConfig/applyAEChartConfig 行协议、applyConfigAction 值变更体、六段序列化通道与 parse/clean/clamp 静态工具逐字搬迁，TE -371 新类 685）；TE 留 getter 门面与 case 4/7/24 跨域后置编排（O2-A06 顺序：值变更→后置→markDirty+markBlockForUpdate）；27 键族 NBT/S35 键逐字不动；死 nextTrackingWindow 副本随迁删除。**含上一执行者中断残留收尾**：半成品 PanelConfigStore+TE diff 评估为可收尾（Store 完整、中断点在 readSyncData 段未替换+旧 config 方法未删），补完 readSyncData 委托并删六私有方法+六静态工具后一次闭合 |
| 2 | `38e3652` | 6 文件，+385/-281 | [SWN-E4] | EUCacheBridge 缓存桥域迁入（updateRequestTick/冷启动/轮询三段顺序不变、cachedEu/cachedEut/cachedStatus/cachedSamples、O2-28 owner 数据集解析缓存三字段+getOwnerDataSet/refreshCachedSamples/formatStatus/tr，TE 1,475→1,293，新类 EUCacheBridge 275 + WindowLabel 70）；S35 四键经 bridge.writeSync/readSync 搬运逐字不动；bindOwner/readPlacementData 改 bridge.onOwnerBound/markDataRefreshNeeded；新增 WindowLabel 枚举统一四套同构 switch（TE.getWindowName/Store.nextTrackingWindow/Render.aeWindowName/Gui.getAEWindowDisplay → shortName/langKey/nextWindow 三映射，常量与 WindowChain 单源同值 0..7，越界回落 5m 与原 default 一致）；O2-28 前波（f3cdbeb）已先行，本批仅随域收编。**设计修正**：蓝图所述「EU/AE 常量双映射」已过时——O2-14 WindowChain 已把两 DataSet 常量单源化同值，枚举直接承载单套映射 |
| 3 | `0e7c219` | 2 文件，+772/-527 | [SWN-E5] | AEMonitorController 网络语义域迁入（tickSampling 门控/sampleAENetwork/pushNow=sendAEMonitorDataToClients/pushAEMonitorOffline(B2-02)/hasNearbyReceiver(O2-21)/clearAEData、标签页与绑定操作族、客户端镜像三缓存、dataKey/aeKey 静态工具、readTabState/writeTabState/readSync/writeSync，TE -450 新类 695）；gridProxy 调用权留 TE 经 AEQuery 匿名实现注入（深查 §三-2），广播端口静态单例留 TE 经 broadcastAEMonitorData 出口转发（CommonProxy.init 注入面不变），TE 留操作族门面单行委托（网络包/双 Block/ClientProxy/Render/Gui 零改动，实测外部引用三处 javadoc 文本不受影响）；区块 NBT 与 S35 的 AE 段键名逐字不动 |

### 九-2 硬约束合规与设计取舍

- **NBT/S35 键逐字不动**：E3 27 键族、E4 四键（cachedEu/cachedEut/cachedStatus/samples）、E5 AE 段（currentTab/chartItem/chartFluid/monitoredItems/monitoredFluids/aeChartSamples）全部经域对象 read/write 方法「段搬运」，键字面量与守卫语义逐字一致；NBT 1.7.10 HashMap 键序无关，装配键集合不变即字节等价。
- **updateEntity 五段顺序**：proxy NBT→onReady→rebuildScreen→EU 轮询（bridge.tickServer）→AE 门控（controller.tickSampling），E4/E5 仅收编第 4/5 段方法体，编排头与顺序未动。
- **依赖方向**：TE→{structure,store,bridge,controller}；bridge→store（B→D 读 displayMode/trackingWindow）；controller→store（C→D 读 aeTrackingWindow）+core（world/坐标/广播出口）；store→仅 DirtyListener 回调；无环。core 引用模式与 E2 ScreenStructure 同构（worldObj 经 public getWorldObj() 访问，protected 直访编译期已验证不可行）。
- **代理与端口归属**：gridProxy 生命周期/查询（isAEConnected/getAEItemAmount/getAEFluidAmount）与 broadcastPort 静态单例均留 TE；controller 经 AEQuery 接口与 core.broadcastAEMonitorData 出口间接使用，域对象不感知 proxy 与端口单例（E6 门面二阶段时可再下沉）。
- **E3 中断残留处置**：按预授权回滚条款评估后走「补完」路径而非回滚重做——半成品已达成蓝图 O2-A03 签名与键面，残留仅 readSyncData 段替换与方法删除，重做反而引入两次相同 diff。

### 九-3 验证与核对

- 四个 commit 均提交前 `./gradlew spotlessApply build` BUILD SUCCESSFUL（含 checkstyle/jar/reobf）；E4 首建 worldObj protected 直访 6 处编译错→改 core.getWorldObj() 后全绿；spotless 两处自动折行。
- 门面调用面实测：getAEKey（GuiAEMonitorList/Render）、getAEMonitorDataKey（BlockNetworkInfoPanel）、setCurrentTab/setChartItem/setChartFluid/clearAEBinding/clearAllAEMonitors/toggleItemMonitor/toggleFluidMonitor（PanelActionQueue/双 Block/Gui）、receiveAEMonitorData（ClientProxy）、getAEChartSamples/getAEMonitorLatest/getAEMonitorAvg300s（Render/双 Gui）全部经 TE 门面签名不变；sampleAENetwork/sendAEMonitorDataToClients 的三处外部出现均为 javadoc 文本。
- TE 行数轨迹：1,998（v1.6.27 基线）→E0 后 1,846→E2 后 1,846→E3 后 1,475→E4 后 1,293→E5 后 **843**（蓝图预估 ≈450+门面，超额保留门面与 proxy/编排所致，E6 门面二阶段可再收敛）。
- 人工冒烟待办（后续进游戏执行）：旧存档读回（27 键族配置+AE 绑定+走势样本完整）、S35 断线重连（配置/缓存/AE 三段同步）、EU/AE 窗口切换八档环进与按钮显示（WindowLabel 三映射）、displayMode 切换即时重算状态文本（case 7 后置）、AE 采样推送/断网空推送清显示/重连恢复（E5 路径经 controller）、面板操作族（切页/绑定/清空/加减监控）行为与此前一致。

### 九-4 待后续轮 / 登记（批次表就绪待后续轮）

- **E6**（门面二阶段：Render/Gui/包类改引 Store/Controller/Bridge 后删 TE 门面，与 R5 同批或其后）——**待后续轮登记**。
- **R 线**（R1/R2/R3/R5 渲染/GUI 结构拆分；E4 已先行统一窗口枚举，O2-A07 ClientChartFormat 的 windowShortName 合流点已就绪）——**待后续轮登记**。
- **B08**：tile↔quantum 拆环已由 B 线并行波次落地（commit `360bd6f`，AnchorReachability+QuantumNodeTypes，本波工作树实测确认）——B 线波次自行登记，此处仅同步状态。**B10**（items↔hud 拆环）亦已由 B 线并行落地（commit `360938c`，HudState+HudController，本波 docs 提交前实测确认）——同样 B 线波次自行登记。
- **O2-22/23**（广播裁剪第二刀）：前置 E5 已闭合，改造落点现为 AEMonitorController.pushNow（原 sendAEMonitorDataToClients）——**待后续轮登记**。
- 其余维持既有登记：O2-12 独立 commit、O2-24 等人工实测、wiki 顺延登记（含本轮 E2-E5 对相关词条的 file:line 影响）。

---

## 十、B 线收束批 B08 + B10 + O2-22/23 评估（波次 wave8-swn-b 处置，本节由该波次追加）

- **输入依据**：`plan/全局调查-优化建议-第二轮.md` 附篇 B O2-B08/O2-B10（:1092-1116）与主篇 §3.7 O2-22/O2-23 + §八-4/§九-4 待后续轮登记（B08/B10/O2-22/23）。
- **派发范围**：B08（tile↔quantum 拆环）→ B10（items↔hud 拆环）两批独立 build；O2-22/23（原「押后」的网络优化第二波）按登记描述评估。
- **基线与并行**：`b32d284` 起步与 E 线并行——本波 B08 `360bd6f` 与 E 线 E4（38e3652）→E5（0e7c219）、B10 `360938c` 交错提交，文件集零重叠；两批 build 各自等待对方工作树编译窗（B08 前 EUCacheBridge worldObj 6 错、B10 前 AEMonitorController 未建成，均为 E 线在途中间态），对方提交后全绿收束，无跨波裹挟。

### 十-1 本波 commit（2 项，均 `./gradlew build` BUILD SUCCESSFUL 后提交）

| # | commit | --stat | 条目 | 处置内容 |
|---|--------|--------|------|----------|
| 1 | `360bd6f` | 5 文件，+197/-47 | [SWN-B08] | **tile↔quantum 拆环（环 #1 闭合）**：① 新增 `quantum/AnchorReachability` 纯判定（量子化[requireQuantized]/区块已加载/是 ME 控制器/proxy 就绪[requireProxyReady] 四段按 tryConnect 原判序单源，tryConnect 判定段缩为 switch——NOT_QUANTIZED→ANCHOR_NOT_QUANTIZED、CHUNK_UNLOADED/NOT_CONTROLLER→ANCHOR_UNREACHABLE、PROXY_NOT_READY→NETWORK_NOT_READY 离线原因映射逐字保持；QuantumNetworkData.assemble 2-3 段改 requireQuantized=false/requireProxyReady=false 同源调用——装配不查量子化、proxy 就绪由 getGrid 的 GridAccessException 兜底两口径均保持；NO_ANCHOR 与维度解析留调用侧——两调用点取 world API 不同[DimensionManager.getWorld vs worldServerForDimension]逐字保留；TileEntity 静态类型 cast 规避 AE2 可选接口解析的编译坑注释随迁）；② 新增 `quantum/QuantumNodeTypes` 小注册表（grid.getMachines 需具体 Class 无法纯接口化，TEQNode 类加载期 static 块注册自身 Class——BlockRegistrar.registerTileEntity preInit 即加载早于任何网格枚举；QuantumNetworkStatsCache :92 改经注册表取用 + null 零节点防御；usedChannels 单节点取连接 max 口径与循环体逐字不动——该循环只触 AE2 API 无 tile 耦合，蓝图所述「usedChannels 提取器注册」经评估不必要）；③ quantum→tile import 归零（实测 grep = 0；登记基线 2 处已随 O2-16/17 波自然消减 1 处，本波闭合最后 1 处 QuantumNetworkStatsCache） |
| 2 | `360938c` | 7 文件，+740/-585 | [SWN-B10] | **items↔hud 拆环（环 #5 闭合）**：① 新增 `common/hud/HudState` 值对象（153 行：原 WirelessMonitorHUD 11 static 可变态 + O2-27 parsedOwnerUuid + EUDataSet 单例实例化，setEnabled/setDisplayMode/setCachedOwnerUUID/clearCache/世界切换 UI 重置字段级操作逐字搬迁）；② 新增 `common/hud/HudController`（491 行：onRenderOverlay ①-④ 业务段——世界切换状态机/20t 背包复合扫描/开关与 10s 真实时间 gap 检测/100t updateCache，tick 口径、B2-04 换绑清 dataSet、O2-27 解析缓存不变量、Baubles NoClassDefFoundError 降级逐字搬迁；receiveSyncedEU 实例化随迁；euText/eutText/realtimeEutText 三 getter 供渲染只读）；③ WirelessMonitorHUD 缩为事件订阅 + ⑤ GL 绘制（683→122 行，经构造注入共享控制器实例，唯一实例持有点 = ClientProxy 字段）；④ PortableWirelessNetworkMonitor.toggleHUDMode 客户端分支改 `GTSimpleWirelessNetwork.proxy.toggleHudMode(mode, owner)` 意图调用（CommonProxy 空实现/ClientProxy 转 applyHudToggle，同 tick 生效语义不变，与包 1/4/6 @SidedProxy 范式统一），**items→hud 客户端类字节码引用归零**（类加载安全由「不执行即不解析」惯例升级为结构保证）；⑤ HUD 读监视器 NBT 改经物品侧语义门面 isMonitorBound/getOwnerUUID/getHudMode（新增 public static，绑定口径与物品侧 isBound 一致且无 ensureNBT 副作用，BUG-8 未绑定不参与 HUD 判定口径保持）；⑥ 蓝图步骤③（common/hud→client.hud 迁包或 SideOnly 标注）登记后置未动。§九-4 所记「B10 待后续轮」由本批落地收讫 |

### 十-2 O2-22 / O2-23 评估（押后维持，登记后续轮）

- **O2-22 走势重叠窗增量**：**押后维持**。前提逐条核对：a) 依赖「O2-20 验证一版后」——O2-20（85850ed）与本轮各批同轮落地，尚无版本间验证与实测包体基线，前提未成立；b)「建议排 O2-04 之后（同区域互踩）」——E5（0e7c219）已闭合、落点已迁 `AEMonitorController.pushNow`（§九-4 已同步），结构性前提成立，但该文件为 E 线当日新产出且 E6（门面二阶段）未收束，本波实施即与 E 线活跃文件互踩（B 线禁触 E 线文件）；c) 风险登记「中（客户端合并+失步检测）」——K=3 增量 + 三重全量兜底 + 丢包空洞检测为协议语义变更，需 O2-20 验证版作基线。**后续轮条件：E 线收束（E6 完）+ O2-20 经一版验证。**
- **O2-23 监控列表 flag 门控值（c-lite）**：**押后维持，跟随 O2-22**。依赖「O2-20 后、视 O2-22 落地后包体实测」——O2-22 未落地、无实测数据，「监控段占包体 74~77%」省益判断与「amount 与 avg 双未变才省」门控设计均需 O2-22 后实测基线，前提不成立。
- **锚点证据（当前树实测）**：服务端装配/推送 `common/panel/AEMonitorController`（pushNow :104 起，原 TE sendAEMonitorDataToClients 逐字迁入）、客户端清空重填 AEMonitorController.receiveAEMonitorData :537 + ClientProxy.handleSyncAEMonitorData + PacketSyncAEMonitorData monitorLatest/monitorAvg300s 段——全部位于 E 线独占/关联文件；与 §五「O2-22/23 押后（登记建议排 O2-04 后）」、§八-4/§九-4 登记一致。

### 十-3 硬约束合规与验证

- **NBT/S35 键逐字不动**：B08 零序列化面（tryConnect 判定序/离线原因映射与 assemble 早退路径行为等价，usedChannels/totalChannels 计算逐字）；B10 零序列化面（HUD 全内存态，物品 NBT 键名与读写逐字不动，门面仅收口读取路径）。
- **PanelActionQueue 主线程队列语义（B07 终态）不回退**：B08/B10 均不触 network/PanelActionQueue（B10 仅改 WirelessEURequestQueue javadoc 一处方法名指向 HudController.scanMonitorInInventory）。
- **jabel 合规**：全部 Java≤8 stdlib、零新依赖；QuantumNodeTypes 的 volatile 单字段为 JLS 基础语义。
- **验证**：两 commit 提交前 `./gradlew build` BUILD SUCCESSFUL（spotlessJava/checkstyle/jar/reobf 全过）；每批 `git status --porcelain` 自审后显式 add，E 线并行改动零裹挟。拆环实证：quantum→tile import = 0；common/items 包 hud import = 0（仅 main/ClientProxy 经 @SidedProxy 客户端组合根持有，方向合法）。
- **人工冒烟待办**（后续进游戏执行）：量子节点建连与四离线原因提示（未量子化/维度未加载/区块未加载/锚点被拆——B08 switch 映射）；终端频道 used/total 显示与 128 满配正确（StatsCache 经注册表枚举）；便携监测终端右键三态切换 HUD（关/常规/科学）即时生效、Shift 改绑清旧 owner 数据集、失去监视器 HUD 关闭清缓存（B10 经 proxy/控制器路径）、Baubles 饰品栏扫描与无 Baubles 降级。

### 十-4 待后续轮 / 登记

- **O2-22/23**：条件与证据见十-2（E 线收束 + O2-20 验证版），落点 AEMonitorController.pushNow + PacketSyncAEMonitorData + 客户端合并——待后续轮登记。
- **B09 完整形态**（TerminalBinding + EventHandler instanceof 门控去除）：维持既有登记待后续轮。
- **B10 蓝图步骤③**（common/hud→client.hud 迁包或 SideOnly 标注 + wiki 登记）：本波维持现位，待后续轮与 R 线窗口合并评估。
- 其余（O2-12、O2-24 人工实测、wiki 顺延登记）维持既有登记。


---

## 十一、终账矩阵（2026-08-22-r2，75 项三态核对）

本节为本文件最终总账；此前各波次段落是证据正文，本节只做逐项索引，状态口径严格为「修复（commit sha）／登记不实施（理由）／降级（证据）」。

### 十一-1 BUG 17 项（B2-01～B2-17）

| 条目 | 三态落账 |
|---|---|
| B2-01 | 修复：由 B07 吸收闭合，`334362a`（PanelActionQueue 主线程队列）。|
| B2-02 | 修复：`75d3628`（断网空推送清客户端陈旧数据）。|
| B2-03 | 修复：`8074e07`（持有绑定终端校验）。|
| B2-04 | 修复：`6f7cf03`（换 owner 清旧数据集与 EU 文本）。|
| B2-05 | 修复：`7c64d8f`（冷却统一主世界时钟）。|
| B2-06 | 修复：`12ec4a9`（dynamo 缓冲饱和累加）。|
| B2-07 | 修复：`66720f5`（安培 double 域与 999A 显示封顶）。|
| B2-08 | 修复：`d8c2c96`（绑定提示服务端门）。|
| B2-09 | 修复：`0138776`（移除区块 NBT/S35 sync 双写）。|
| B2-10 | 降级：不升级 `getAEKey` 格式；WSD 持久化键改动会使旧 series 孤儿化，NBT 键不动约束与既有 monitoredItems 语义均不允许；仅登记后续 GUI 精确匹配文案。|
| B2-11 | 修复：`61eae1c`（删除零消费 `aeMaxSamplePoints`，实际容量为固定 61）。|
| B2-12 | 修复：`4b59d1e`（包 4 三 count 按 61/512 钳制）。|
| B2-13 | 修复：`2a0aafb`（getAll 快照冻结为 unmodifiableSet）。|
| B2-14 | 修复：`7191c1e`（QT_* 五键单源化，编译期常量内联保持 NBT 字节兼容）。|
| B2-15 | 修复：`9fa5b60`（zh_CN 全角冒号 38 行统一半角）。|
| B2-16 | 修复：`f422aca`（删除 `MonitorScanResult.stack` 零消费字段）。|
| B2-17 | 登记不实施：assemble 是否要求量子化属于产品语义裁决；当前数据仍为真实网络数据，维持离线装配口径，待后续产品确认。|

### 十一-2 遗留 4 项登记

| 条目 | 三态落账 |
|---|---|
| BUG-02 | 登记不实施：量子控制器挖掘减速必须完成单人 + dedicated 六用例人工实测后终裁；未以静态证据冒充修复。|
| O2-12 | 登记不实施：红石滞后带重载闪断是行为变更，独立授权与回归后再实施；`3352a9d` 明确保持现状语义。|
| O2-B09 | 登记不实施：TerminalBinding 完整形态及手势门控仍待后续轮，当前 B2-14 `7191c1e` 已完成最低风险 QT 键单源化，不扩大范围。|
| O2-B11 | 登记不实施：采样／装配／推送三段分离依赖 E6 与后续域边界收束，当前 E5 已闭合但不提前互踩。|

### 十一-3 OPT 58 项逐条索引

**第一轮已落地 18 项**：`SWN-OPT-04`→`99dcb75`、`SWN-OPT-06`→`330cb64`、`SWN-OPT-08`→`330cb64`、`SWN-OPT-12`→`7291abd`、`SWN-OPT-13`→`75644a8`、`SWN-OPT-14`→`d317bc5`、`SWN-OPT-16`→`330cb64`、`SWN-OPT-17`→`f236581`；其余第一轮 10 项为既有 sweep 总账中已核销的历史优化项，均沿用 `plan/处置汇总-20260821-sweep.md` 对应修复 commit（本轮不重复改码）。

**第二轮已落地／核销 40 项**（逐条）：

- `O2-01` 修复 `30365fb` + `b32d284`；`O2-02` 修复 `dfb3fe3`；`O2-03` 修复 `38e3652`；`O2-04` 修复 `0e7c219`。
- `O2-05` 登记不实施：buildUI 拆组待后续 GUI 窗口；`O2-06` 登记不实施：AE2 适配器基类无独立收益，待后续 R 线；`O2-07` 修复 `07f7e2d`；`O2-08` 登记不实施：ClientChartFormat 待 R1；`O2-09` 登记不实施：ChartRenderer 待 R2/R3；`O2-10` 登记不实施：Tab 基类待 R5/E6；`O2-11` 修复 `3352a9d`；`O2-12` 见遗留登记；`O2-13` 修复 `73463ea`；`O2-14` 修复 `fa990d0`；`O2-15` 修复 `a115baa`；`O2-16` 修复 `3625f4b`；`O2-17` 修复 `f0c67c7`；`O2-18` 修复 `711873b`；`O2-19` 跳过：O2-17 已将回包降至固定 30B，10t 更跟手，继续改 20t 边际收益不足；`O2-20` 修复 `85850ed`；`O2-21` 修复 `b66222d`；`O2-22` 登记不实施：需 O2-20 版本间基线、E6 收束及协议实测；`O2-23` 登记不实施：跟随 O2-22，需包体实测；`O2-24` 等人工实测：六用例完成前不实施；`O2-25` 修复 `e2ac30a`；`O2-26` 修复 `46d3f31`；`O2-27` 修复 `ad81e81`；`O2-28` 修复 `f3cdbeb`；`O2-29` 修复 `17f8cfe`；`O2-30` 修复 `193aa0f`。
- `E0`（=O2-29）修复 `17f8cfe`；`E1` 修复 `30365fb`；`E2` 修复 `b32d284`；`E3` 修复 `dfb3fe3`；`E4` 修复 `38e3652`；`E5` 修复 `0e7c219`；`E6` 登记不实施：门面二阶段待后续轮与 R5 合流。
- `R1`（=O2-08）登记不实施：待后续 ClientChartFormat；`R2`（=O2-09）登记不实施：待 R1 后 ChartRenderer；`R3`（=O2-09）登记不实施：待 R2 后 AE 接入；`R4`（=O2-07）修复 `07f7e2d`；`R5`（=O2-10）登记不实施：待 E6/GUI 门面收束。
- `B07` 修复 `334362a`；`B08` 修复 `360bd6f`；`B09` 见遗留登记；`B10` 修复 `360938c`。

> 计数说明：第一轮 18 + 第二轮 40 = OPT 58；E/R/B 为同一批次的别名索引，不重复计数。O2-19 的「跳过」按登记不实施的证据化子态记录，O2-24 明确保留人工实测门槛。

### 十一-4 交付债与工作区建议

- wiki 三词条债统一标记「待 wiki 波」：`packet-update-ae-tab-state.md:20`、`network-info-panel-stack-watcher.md:22`、`wireless-energy-monitor-mui2-gui.md:27`；本仓不跨仓编辑 wiki。
- `stash@{0}` 仅作建议保留／后续核对，不执行 pop、drop 或改写；其内容不计入本次 75 项落账。
- 本终账 docs commit 单独提交，代码 commit 与 docs commit 分离；显式 `git add plan/处置汇总-20260822-r2.md`，不 push。
